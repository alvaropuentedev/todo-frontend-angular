import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'todo-frontend-angular',
  appName: 'ToList',
  webDir: 'dist/frontend/browser'
};

export default config;
